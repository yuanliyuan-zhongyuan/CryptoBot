# ============================================================================
# CryptoTrade 学习项目 - Python 依赖（最小运行集）
# ============================================================================
# 说明：
# - 只列出当前 CryptoTrade 子项目实际用到的依赖
# - 分类清晰，后续新增按分类追加，便于维护
# - 与主项目依赖隔离，避免冲突
#
# 使用：
#   pip install -r cryptotrade/requirements.txt
# ============================================================================

# ────────────────────────────────────────────────────────────────────────────
# 🏛️ 核心与配置 (Core & Configuration)
# ────────────────────────────────────────────────────────────────────────────
pyyaml>=6.0,<7.0            # YAML 配置解析（加载策略/交易所/risk 配置）

# ────────────────────────────────────────────────────────────────────────────
# 🔄 异步与网络 (Async & Networking)
# ────────────────────────────────────────────────────────────────────────────
# 当前未使用第三方库，仅使用 asyncio/typing 等标准库
# 后续如接入 httpx/websockets，再在此处追加

# ────────────────────────────────────────────────────────────────────────────
# 🧰 工具与CLI (Tools & CLI)
# ────────────────────────────────────────────────────────────────────────────
# 当前未使用第三方库（入口使用标准库 argparse）
# 后续如采用 rich/typer 增强CLI，再在此处追加

# ────────────────────────────────────────────────────────────────────────────
# 🧪 测试与质量 (Testing & Quality)
# ────────────────────────────────────────────────────────────────────────────
# 后续如需要 pytest/ruff/mypy，再在此处追加

